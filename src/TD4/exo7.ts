function litEntiers(): void {
  let reponse = Number(prompt("Entrer un nombre"));
  let sommeNegatif = 0, compteurPositif = 0;
  let sommePositif = 0, compteurNegatif = 0;

  let maximum = reponse;
  let minimum = reponse;

  if (reponse < 0) {
    sommeNegatif = sommeNegatif + reponse;
  }
}
litEntiers();
