function echange(liste: Array<number>, i1: number, i2: number): number {
  for (let i = 0; i < liste.length; i++) {
  }
}
echange([5, 7, 3, 8], 1, 2);

function melange(liste: Array<number>, n: number): number {}
